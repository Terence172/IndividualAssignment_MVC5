@model IEnumerable<IndividualAssignment_MVC5.Models.comem>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Home/LayoutDashboard.cshtml";
}

<div class="page-header">
    <div class="row">

        <div class="col-md-6 col-sm-12">
            <div class="title">
                <h4>Committee Members - List</h4>
            </div>
            <nav aria-label="breadcrumb" role="navigation">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="@Url.Action("DashboardAdmin", "Home")">Home</a></li>
                    <li class="breadcrumb-item">Manage Committee Members</li>
                    <li class="breadcrumb-item active" aria-current="page">Committee Members List</li>
                </ol>
            </nav>
        </div>
    </div>
</div>


<div class="pd-20 card-box mb-30">
    <div class="clearfix mb-20">
        <div class="pull-left">
            <h4 class="text-blue h4">Committee Members</h4>
            <p>Committee Members registered to the <code>.system</code> :P</p>
        </div>
        <div class="pull-right">
            <a href="@Url.Action("Create", "comems")" class="btn btn-primary btn-sm scroll-click" role="button">
                <i class="icon-copy fa fa-user"></i> Create Committee Member
            </a>
        </div>
    </div>
    <div class="pb-20">
        <table class="table">
            <thead>
                <tr>
                    <th class="table-plus datatable-nosort">Committee Member Username</th>
                    <th>Lecturer Name</th>
                    <th>Academic Programme</th>
                    <th class="datatable-nosort">Action</th>
                </tr>
            </thead>
            <tbody>

                @foreach (var item in Model)
                {

                        <tr>
                            <td class="table-plus">
                                <span>@Html.DisplayFor(modelItem => item.lecturer.user.user_name)</span>
                            </td>

                            <td>@Html.DisplayFor(modelItem => item.lecturer.user.user_firstname) @Html.DisplayFor(modelItem => item.lecturer.user.user_lastname)</td>
                            <td>@Html.DisplayFor(modelItem => item.programme.prog_name)</td>
                            <td>
                                <div class="dropdown">
                                    <a class="btn btn-link font-24 p-0 line-height-1 no-arrow dropdown-toggle" href="#" role="button" data-toggle="dropdown">
                                        <i class="dw dw-more"></i>
                                    </a>
                                    <div class="dropdown-menu dropdown-menu-right dropdown-menu-icon-list">
                                        <a class="dropdown-item" href="@Url.Action("Details", "comems", new { id = item.comem_id })">
                                            <i class="dw dw-eye"></i> View
                                        </a>
                                        <a class="dropdown-item" href="@Url.Action("Edit", "comems", new { id = item.comem_id })">
                                            <i class="dw dw-edit2"></i> Edit
                                        </a>
                                        <a class="dropdown-item" href="@Url.Action("Delete", "comems", new { id = item.comem_id })">
                                            <i class="dw dw-delete-3"></i> Delete
                                        </a>
                                    </div>
                                </div> 
                            </td>
                        </tr>
                    
                }
            </tbody>
        </table>
    </div>
</div>
